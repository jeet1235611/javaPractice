package linkedlist2;

public class Node {
	int data;
	Node next;
	//Constructor to initialise values in the beginnning itself.
		Node(int data) {
		this.data=data;
		//next = null; This thing not needed since by default hi null hota h
		}
}
